url: "https://pi-tv.github.io"

collections:
  docs:
    output: true

defaults:
  - scope:
      type: docs
    values:
      layout: doc
      permalink: /:title

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable : true

plugins:
  - jekyll-sitemap
  
